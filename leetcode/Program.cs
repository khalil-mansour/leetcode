using System;


namespace leetcode
{
    class Program
    {
        static void Main(string[] args)
        {
            // new LinkedListPalindrome().IsPalindrome(new ListNode(1, new ListNode(3, new ListNode(1, null))));

            // new RomanToInteger().Convert("MMXC");

            // new RansomNote().CanConstruct("assembly", "basimtely");

            // new FizzBuzz().FBResult(15);

            // new MiddleLinkedList().MiddleNode(new ListNode(1, new ListNode(7, new ListNode(8, new ListNode(3, new ListNode(4, null))))));


            // int[][] jagged =
            // {
            //     new int[] { 1, 1, 0, 0, 0 },
            //     new int[] { 1, 1, 1, 1, 0 },
            //     new int[] { 1, 0, 0, 0, 0 },
            //     new int[] { 1, 1, 0, 0, 0 },
            //     new int[] { 1, 1, 1, 1, 1 }
            // };

            // new KWeakestRowsInMatrix().KWeakest(jagged, 3);

            // new NbOfStepsToReduceToZero().Steps(59);

            // new RichestCustomerWealth().Richest(jagged);

            // var res = new ValidParenthesis().IsValid("([{}({[()]})])");

            // ListNode list1 = new ListNode(1, new ListNode(3, new ListNode(4, new ListNode(6, new ListNode(9, null)))));
            // ListNode list2 = new ListNode(1, new ListNode(2, new ListNode(5, new ListNode(7, new ListNode(8, null)))));

            // new MergeSortedLists().Merge(list1, list2);

            // new MaximumSubarray().MaxSub(new int[] { -2, 1 });

            // new MergeSortedArray().Merge(new int[] { 6, 14, 17, 0, 0, 0 }, 3, new int[] { 2, 5, 6 }, 3);

            // new LinkedListCycle().HasCycle(a);

            // new MinStack().GetMin();

            // new IntersectLinkedList().GetIntersectionNode(a1, b1);

            // new MajorityElement().Majority(new int[] { 6, 5, 5 });

            // new FactorialTrailingZeroes().TrailingZeroes(31);

        }
    }
}
